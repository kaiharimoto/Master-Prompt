name: mp_runner
description: Desktop process supervision for Master Prompt — Claude Code CLI discovery, capability probing, stream-json ingestion, limit detection and durable auto-resume. dart:io only, no Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0
  mp_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
