name: mp_core
description: Domain core for Master Prompt — mission spec, prompt compiler, interview engine and continuity protocol. Pure Dart, no Flutter, no dart:io.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0


dependencies:
  collection: ^1.19.0
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
