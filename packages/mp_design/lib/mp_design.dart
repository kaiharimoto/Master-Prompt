/// Design system for Master Prompt.
///
/// Near-monochrome, hairline-ruled, on an 8-point grid, set in Inter Medium.
/// The app produces long technical documents, so the interface stays quiet:
/// colour is reserved for the few things that must interrupt.
library;

export 'src/theme.dart';
export 'src/tokens.dart';
export 'src/widgets.dart';
